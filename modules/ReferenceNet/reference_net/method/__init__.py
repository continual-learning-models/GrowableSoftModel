"""method — L2, the soft-model methodology as DATA
(next-dev-docs/52 v1.7 section 2.1/6): preset spec bundles and
admission gates. Swappable wholesale without touching L1
(AR-2); imports foundation (and numpy/stdlib) ONLY — enforced
by the V-A0-2 linter. Policy VALUES flow in from the hosts as
arguments; this package never reads growthpolicy itself.
"""
