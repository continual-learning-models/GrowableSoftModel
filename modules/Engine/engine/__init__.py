"""ENGINE — the lib's shared computation layer (L0): math
primitives, numpy/torch kernels (backends/), loop-block kernel
math (loop_ops), and the generic self-processing machinery (spu/).
No models live here."""
