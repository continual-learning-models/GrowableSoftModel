# experiments — pre-registered E-series drivers

Every experiment: spec.md committed BEFORE driver.py runs; 3 seeds
{7,8,9}; equal budgets; >=2/3-seed rule; raw results committed even on
failure; interpretations fixed in the spec, never re-spun.
